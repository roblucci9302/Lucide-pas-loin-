# ==========================================
# LUCIDE - CONFIGURATION ENVIRONNEMENT
# ==========================================
#
# IMPORTANT:
# 1. Copier ce fichier vers .env.local
# 2. Remplir avec vos vraies clés secrètes
# 3. NE JAMAIS commiter .env.local sur Git!
# ==========================================

# ── PORTKEY API ─────────────────────────────
# Clé API principale Portkey (obtenue depuis portkey.ai)
PORTKEY_API_KEY=your_portkey_api_key_here

# ── FIREBASE CONFIGURATION ─────────────────
# Configuration Firebase (obtenue depuis Firebase Console > Project Settings)
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ── OPENAI API (optionnel) ─────────────────
# Clé API OpenAI directe (si non utilisée via Portkey)
OPENAI_API_KEY=sk-your_openai_key_here

# ── ENVIRONNEMENT ──────────────────────────
# development | production | test
NODE_ENV=development

# ── SECURITY ───────────────────────────────
# Niveau de logging (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# ==========================================
# INSTRUCTIONS POUR DÉMARRER:
# ==========================================
# 1. cp .env.example .env.local
# 2. Éditer .env.local avec vos vraies clés
# 3. npm run dev
# ==========================================
